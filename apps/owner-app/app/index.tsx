import { AppScreen, EmptyState } from '@tuljai/ui';

export default function OwnerHomeScreen() {
  return (
    <AppScreen>
      <EmptyState
        title="Tuljai Stays Owner"
        description="Owner app foundation is ready for authentication and property modules."
      />
    </AppScreen>
  );
}
