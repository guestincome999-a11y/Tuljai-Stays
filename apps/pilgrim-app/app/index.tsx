import { AppScreen, EmptyState } from '@tuljai/ui';

export default function PilgrimHomeScreen() {
  return (
    <AppScreen>
      <EmptyState
        title="Tuljai Stays"
        description="Pilgrim app foundation is ready for authentication and discovery modules."
      />
    </AppScreen>
  );
}
